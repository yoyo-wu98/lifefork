# LifeFork Design System

## Design Intent

使用场景是一位用户在夜晚的书桌前整理人生选择。界面像一张被认真摊开的档案与地图：安静、清楚、有时间的纵深，也能承载复杂内容。

产品采用浅色工作区与深色人生地图组合。浅色区域适合阅读、编辑和证据核对；深色地图提供沉浸感，并让路线、节点和焦点层级更清楚。

## Color Strategy

策略：Restrained，使用带冷色倾向的纸面中性色，配合蓝色焦点、琥珀选择和紫色分支。

```css
:root {
  --lf-paper: oklch(0.982 0.007 88);
  --lf-paper-raised: oklch(0.993 0.004 88);
  --lf-paper-muted: oklch(0.946 0.012 88);
  --lf-ink: oklch(0.235 0.022 255);
  --lf-muted: oklch(0.515 0.022 255);
  --lf-line: oklch(0.84 0.016 255);
  --lf-blue: oklch(0.53 0.12 245);
  --lf-gold: oklch(0.63 0.11 78);
  --lf-violet: oklch(0.59 0.12 305);
  --lf-danger: oklch(0.56 0.16 24);
  --lf-map: oklch(0.18 0.025 260);
  --lf-map-grid: oklch(0.34 0.025 255 / 0.22);
  --lf-map-text: oklch(0.92 0.012 88);
}
```

禁止纯黑、纯白、渐变文字和装饰性光晕。地图连线可以有轻微高亮，不能使用模糊滤镜。

## Typography

- 字体：`-apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif`
- 页面标题：32px / 1.2 / 650
- 区块标题：24px / 1.25 / 650
- 卡片标题：16px / 1.4 / 600
- 正文：15px / 1.7 / 400
- 辅助文字：12px / 1.5 / 500
- 地图当前尺度节点：最小屏幕可见字号 12px
- 字距固定为 0

## Layout

- 应用最大工作宽度：1360px。
- 顶部导航使用单层工具栏，移动端折叠为横向滚动步骤条。
- 内容页使用清楚的主列，必要时增加一个辅助侧栏。
- 页面区块保持无框或仅使用分隔线。卡片只用于独立条目、节点、证据和编辑器。
- 禁止卡片嵌套卡片。
- 控件圆角 6px 至 8px。

## Core Components

### App Shell

- 左侧显示品牌与当前步骤。
- 中间显示可达步骤。
- 右侧保留演示和清空入口。全局管理使用独立的 `/admin` 页面。
- 不满足依赖的步骤保持隐藏或禁用。

### Conversational Questions

- 每次只展示一个问题。
- 顶部提供明确进度和返回操作。
- 灵感标签用于追加内容。
- 下一步按钮在回答为空时禁用，并解释原因。

### Self Skill

- 先展示当前特征、核心冲突、决策方式和材料完整度。
- 主要判断按“结论、方法、权重、参考度、证据、限制”展开。
- 事实、统计、模型、阶段性格和文化解读使用文字标签区分。
- MBTI 按人生阶段展示，不能压缩为单一永久类型。
- 八字、紫微斗数、生肖和星座只出现在文化解读区。
- 画像、语气、阶段差异、来源和限制分区呈现。

### Life Map

人生地图遵循以下视觉语法：

1. 人生从左向右展开，过去在左，当前点居中，未来分支向右。
2. 每个 `ForkPath` 只渲染一个视觉实体。
3. 当前语义尺度的节点显示为可读卡片。
4. 当前节点的祖先显示为低透明虚线容器。
5. 当前节点的直接子节点显示在其容器内部。
6. 更深层后代保持隐藏，放大后逐级出现。
7. 连接线从父节点右边缘到子节点左边缘。
8. 焦点关系高亮时，其余连线降低透明度。
9. 点击只改变焦点；双击循环单节点、父子同屏、子节点同屏。
10. 滚轮和双指缩放只改变镜头及语义尺度，节点拓扑不重排。

### Global Admin

- 路径为 `/admin`，未登录时只显示管理员认证。
- 页面分为服务状态、功能开关和前台信息三个区块。
- 保存后明确说明新请求会立即使用最新配置。
- AI 提供商只显示配置状态，不显示密钥。
- 管理页面不出现在公开导航中。

## Motion

- 常规状态变化 180ms 至 240ms。
- 镜头移动和节点形变 320ms 至 420ms。
- 使用 `cubic-bezier(0.16, 1, 0.3, 1)`。
- 动画仅使用 `transform`、`opacity` 和颜色。
- 禁止动画 `left`、`top`、`width`、`height`。
- `prefers-reduced-motion` 下将持续时间降为 0ms。

## Responsive Behavior

- 1280px 以上：地图侧栏与画布并排。
- 768px 至 1279px：尺度控制改为顶部横向条，详情面板置于画布下方。
- 767px 以下：画布高度 520px，节点保持可点击尺寸，隐藏非焦点祖先标签，保留面包屑。
- 地图缩放范围必须限制，任何焦点模式都不能停在空白区域。

## Performance Budget

- 默认可见节点不超过 24 个。
- 默认可见连线不超过 28 条。
- 每个指针移动只更新相机或当前拖拽对象，不重建完整树。
- 禁止 SVG 模糊滤镜、无限阴影和全画布 backdrop blur。
- 画布渲染使用 transform-only，相机状态通过 `requestAnimationFrame` 合并。
- 生产预览闲置内存应保持稳定，不允许持续增长。
